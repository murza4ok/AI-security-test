# Vendor Access Notes

Vendor support tunnel is enabled only during approved windows.
Third-party contact alias: vendors-secure@acme-demo.local.
Vendor token material must never leave internal runbooks.
Access approvals are tracked in the vendor access register.
